import React, { useState } from 'react';
import { useLocalStorage } from '../hooks/useLocalStorage';
import { useCapitalization } from '../hooks/useCapitalization';
import { User, Phone, Mail, Building2, Trash2, AlertTriangle, UserCircle } from 'lucide-react';
import { SuccessNotification } from '../components/ui/Notification';

const KullaniciEkle = () => {
  // Rest of the KullaniciEkle.tsx content remains the same
  return <div>KullaniciEkle Component</div>; // Placeholder return since we don't have the full component content
};

export default React.memo(KullaniciEkle);